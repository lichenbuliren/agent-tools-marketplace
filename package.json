{
  "name": "agent-tools-marketplace",
  "version": "0.1.0",
  "private": true,
  "description": "Public Codex marketplace for reusable agent tools.",
  "scripts": {
    "test": "node --test tests/marketplace-contract.test.mjs"
  }
}
